# Claude Code Instructions — Slippi Ranked Stats

## Commits

**NEVER add a Co-Authored-By tag, Signed-off-by, or any line that references Claude or an Anthropic email address to any git commit.**

This causes Claude to appear in the GitHub contributor list. Removing it requires deleting and recreating the branch, which takes a full day for GitHub's cache to clear. There are no exceptions to this rule regardless of how large or complex the change is.

## Security

**NEVER read, print, or otherwise access sensitive files** including `.env`, `.env.*`, `*.pem`, `*.key`, `*.secret`, `credentials.*`, or any file that may contain secrets, API keys, tokens, or passwords. If a task seems to require reading one of these files, stop and ask the user how to proceed instead.

## General

- Read `docs/dev_notes.md` at the start of each session — it is the cross-session handoff document.
- The Set Grading feature ships as a **Premium** feature, gated by `$isPremium` (live since v1.6.0) — it is **not** dev-only.
- The OBS **Live Stats Overlay** ships as a **Premium** feature (live since v1.8.0), gated by `$isPremium` + the in-app toggle. Server-less: the app writes `stats.html` + `stats-state.js` to `<appDataDir>/stream-overlay/` and OBS loads it as a Browser Source. It's an **always-on panel** (tag / rank medal / Rating / global placement / season W/L / today's session record, opponent line during a set) with a **post-set bridge** that shows the set result + grade + the per-set Rating change when a ranked set ends. The **opponent line** (live since v1.8.2) shows the opponent's tag, rank medal + tier-colored name, Rating, and season W–L (green/red) — all from the same `fetchRatingSnapshot(opponentCode)` call already made on set start (`opponent_tier_color`/`opponent_tag`/`opponent_season_*` on `ActiveSet`). **It replaced the standalone v1.7.0 Set Grade Overlay** — the grade is now folded in, and `src/lib/overlay.ts` was removed. Code: `src/lib/stats-overlay.ts` (+ `overlayPreviewHtml` for the in-app iframe preview), `src/lib/rank-medals.ts` (official Slippi rank SVGs in `src/assets/ranks/`, inlined), `statsOverlayPayload`/`statsOverlayPreview` in `store.ts`, app-level write in `App.svelte`. **Each panel element is individually toggleable** (live since v1.8.7) via the `statsOverlayVisibility` store (persisted `srs_statsOverlayVisibility`, defaults all-on, threaded as `show` on the payload and gated by `vis()` in `stats-overlay.ts`); the "Show on overlay" chip row in the setup card drives it.
  - **v1.8.8 overlay changes:** (1) **Opponent characters come from their Slippi profile** — `fetchRatingSnapshot` now returns the profile's `characters` (it was already queried, just discarded); the watcher keeps their top mains (`opponent_chars`, external char ids) and the overlay renders them as **character stock icons** instead of the lagging per-game char (falls back to the live char when the profile lists none). Icons are project-slippi GPL-3.0 stock heads in `src/assets/characters/` (external-id-named PNGs + NOTICE), inlined as base64 data URIs by `src/lib/char-icons.ts` (same pattern as rank-medals; also holds `EXTERNAL_CHARACTERS`, `API_CHAR_TO_EXTERNAL`, `internalToExternal`). (2) **Rating display split for clarity:** current Rating moved to the identity column (below season W/L), the "Today's stats" block shows the **session** change, and the post-set bridge shows a separate **"THIS SET"** Rating change (uses `OverlaySetResult.ratingBefore` vs the refetched rating). (3) **"MMR" → "Rating"** in all user-facing overlay/UI copy (matches slippi.gg). (4) Live **"Set Count:"** scoreboard row during a set. (5) **Overlay self-reloads after an app update** — `OVERLAY_VERSION` (hash of the rendered page) is stamped into each state write as `htmlVersion` and baked into the page as `PAGE_VERSION`; on mismatch the page cache-bust-reloads itself, so no manual OBS Browser-Source "Refresh" is needed. `App.svelte` re-writes `stats.html` whenever `OVERLAY_VERSION` changes (before announcing it) so there's no reload loop.
- **Auto-scan on launch (v1.8.8, all users):** `App.svelte` now runs an incremental `scanDirectory` in the background on startup so replays played while the app was closed get ingested without a manual scan (the watcher only catches files created while running). `insertGame` is `INSERT OR IGNORE` on a UNIQUE filename, so it can't double-insert alongside the watcher.
- **v1.8.9 changes:** (1) **Matchup baseline coverage restored for ALL 25 characters.** A prior benchmark rebuild (commit `b7bb93a`) re-ran `scripts/parse_hf_replays.py` with the default `--character FALCO` instead of `--character ALL`, silently dropping matchup buckets for 22 characters — so `gradeSet`'s `by_matchup[player][opp]` only had data when the player OR opponent was Fox/Falco/Marth; every other matchup fell back to `by_player_char`. Fixed by a full `--character ALL` rescan (HuggingFace `erickfm/slippi-public-dataset-v3.7`, ~192k games) + `regen_benchmarks.py`. 21/24 player chars now have broad matchup coverage (≥50-sample matchups make it into `grade-benchmarks.ts`; the underlying `grade_baselines.json` keeps ≥20). (2) **Flawless rate ⇒ score 100.** `percentileScore` in `grading.ts` gained a saturated-ceiling remap (new `max` param + `BOUNDED_RATE_STATS` set): when a [0,1] rate stat's p95 sits at the 1.0 ceiling (e.g. recovery), a perfect value now reaches 100 instead of capping at 95; non-saturated stats (edgeguard etc.) are unchanged. `GRADING_LOGIC_VERSION` bumped 4→5 (forces a regrade of existing grades). (3) **Two new overlay toggles** (`setResult`, `setRating` on `OverlayVisibility`): the post-set **result line** and the per-set **Rating change** are now independently hideable — the latter decoupled from the `mmr` (current-Rating) toggle. (4) **Stacked-layout reorder:** `buildStacked` now keeps the Today's block with the persistent identity at the top and pushes the transient `contextHtml` (opponent line / post-set grade+result) to the bottom (the side layout already did this). (5) **"THIS SET" → "LAST SET:"** label on the post-set rating change. The Ice Climbers / Nana raw-stat distortion noted here was **FIXED in v1.8.10** (see next bullet) — both parsers now fold the follower in.
- **v1.8.10 changes:** (1) **Ice Climbers / Nana distortion FIXED (all users).** Both parsers (`slp_parser.ts` + `parse_hf_replays.py`) previously discarded the follower (Nana), so hits/damage/edgeguards on her were invisible — distorting IC matchups in both directions. Now the follower is folded in. `slp_parser.ts`: `parseEventStream` collects `followerFrameData` (a GAP in frame numbers = Nana dead — the only reliable death signal, since her `stocks` field mirrors Popo's shared stock); `computeConversionStats` treats the IC entity as in-stun/in-control if EITHER climber is, using combined Popo+Nana percent for multi-hit tracking (kills stay Popo-only — Nana ≠ a stock); a new `countFollowerTrips` helper folds Nana's offstage trips into edgeguard (opponent's Nana) and recovery (player's Nana). `parse_hf_replays.py` mirrors this via `_follower_lists`/`_follower_in_stun`/`_nullable_mask`/`_follower_percent_array`/`_follower_damage`/`_follower_offstage_trips` (peppi NULL-PADS the follower to leader length; nulls = dead). Stage control stays Popo-only. `GRADING_LOGIC_VERSION` bumped 5→6. ⚠ Known/accepted: `openings_per_kill` can move either direction for IC — the combined-entity model treats the team as "in a conversion" if either climber is in stun, so juggling both merges into one extended punish; a new opening only starts when the whole team was neutral (standard Slippi conversion model on two bodies; identical in both parsers so grades stay fair). (2) **Full `--character ALL` benchmark rebuild — 221,602 games (up from 192,307).** The v1.8.9 scan was throttled (~2 files/sec, timed-out downloads silently skipped); this clean run (~10/sec) ingested ~29k more games across the WHOLE cast — every character gained samples, matchup combos 258→423 (≥20 in `grade_baselines.json`), `grade-benchmarks.ts` →283 (≥50). **So this release re-grades ALL characters, not just IC.** (3) **Telemetry owner-exclusion client kill-switch goes live** (committed earlier in `580db90`): `pingTelemetry()` no-ops on `import.meta.env.DEV`, `isOwner` (verified Discord id === `OWNER_DISCORD_ID`), or a manual `srs_telemetryOff=1` flag.
- **v1.8.11 changes:** **Merged-baseline grading release (data-only; no app-logic change).** `scripts/grade_baselines.json` + `src/lib/grade-benchmarks.ts` were regenerated (in the pull merged this session) from the new `raw_stats.sqlite` StatsDB sidecar (multi-GB, gitignored, other-Mac-only): **2,129,888 samples** = 1.70M from `erickfm/melee-ranked-replays` (all 934 tarballs, plat+ ranked) + 430k from the v3.7 rescan — ~5× the previous pool. Matchup entries in `grade-benchmarks.ts` **283→569** (≥50 samples; baselines JSON keeps 636 at ≥20); all 26 player chars now have matchup buckets (569/676 combos). `BENCHMARKS_VERSION` = generation timestamp (`2026-07-14T14:49:00…`) so all stored grades regrade on update. `GRADING_LOGIC_VERSION` unchanged at 6 — the ONLY variable vs v1.8.10 is the benchmark distribution. Grade impact (validated against Joey's 891 sets, 891/891 recompute match): mean −0.7 pts, 89% keep their letter, shift concentrated in Defense (ranked pool survives ~10% longer per stock); Neutral/Punish flat. `by_rank` sections (platinum/master/diamond) are captured in the sidecar for a future rank-stratified upgrade (re-pooling = a query, no rescan). Pipeline code (`--dataset ranked` scan, `scripts/stats_db.py`, watchdog, `--dataset db` builder) also landed in the pull.
- **v1.8.12 changes:** community PR #3 (Bruno Albero) stopped `slp_parser.ts` discarding `mode.direct` replays, so direct-connect games reach the **Unranked & Direct Stats** tab (new `directGames` derived store). It only caught replays created *after* updating — see the v1.8.13 backfill.
- **v1.8.13 changes:** (1) **Live session tracking now covers unranked + direct, not just ranked.** The gate was one line in `watcher.ts` (`match_type === "ranked"`); `handleRankedGame` is now `handleLiveGame` and `ActiveSet` carries a `mode: LiveMode` (`"ranked" | "unranked" | "direct"`, in `store.ts`). **The critical asymmetry: a `match_id` means different things per mode.** In ranked it's one best-of-3 set (2–3 games); in unranked/direct it's the ENTIRE connection with that opponent — verified against Joey's DB + replay archive at up to **53 games (unranked)** and **62 (direct)** under one `match_id`. So only ranked can "complete": grading, `setResultFlash`, the overlay post-set bridge and `scheduleSnapshotFetch` are all gated behind `isRanked`, because a phantom rating refetch would write junk snapshots into the rating history for modes that don't move Rating. `liveSetRecord` now filters to ranked; unranked/direct get `liveUnrankedRecord`, counted in **games** (never summed with sets). `computeAllTimeRecord` + `getGamesVsOpponent` are mode-aware (sets for ranked, games otherwise; `all_time_unit` on `ActiveSet`). Unranked/direct runs have **no end event**, so `armIdleClear` (15 min) clears a quiet run, and a resumed run with the same `match_id` rebuilds the card. **No mode indicator anywhere** — deliberate, Joey rejected a chip on the overlay/card; the only visible difference is the scoreboard caption (`Set Count:` vs `Games:`). (2) **Opponent now appears at game START, not game end** — `parseSlpHeader` in `slp_parser.ts` reads the Game Start (0x36) block alone. The normal path takes connect codes from the trailing `metadata` block (byte ~3M of a 3 MB file, written only at game end), and the parse is debounced 800ms after the last write, which never fires mid-game at 60fps. Codes live at **payload offset 544 + 10×port**, char ids at `0x64 + 0x24×port`, **Shift-JIS with a FULL-WIDTH `＃` (0x81 0x94)** — an ASCII `#` search over the header finds nothing, which is why this was easy to miss. Verified 297/299 against real replays. `peekHeader` in `watcher.ts` fires on the fs `create` event with a retry backoff (file can exist before the header flushes) and fails closed (`null`, never a guess); `handleLiveGame` **carries over** the already-fetched profile so the card doesn't blank at game end, and skips the redundant second API call. **Tag + character come from the header too** — netplay display names are 31-byte Shift-JIS fields at **payload offset 420 + 31×port**, so the tag needs no API round-trip and survives an API outage; `char-icons.ts` gained `externalToInternal()` (mirror of `internalToExternal()`, matched by NAME, round-trip tested over all 26 chars). ⚠ That conversion lives in `watcher.ts`, NOT `slp_parser.ts` — that module has no imports by design and importing char-icons would cycle (`slp_parser → char-icons → parser → slp_parser`) with eagerly-built tables initialising empty, so `parseSlpHeader` returns `*_char_external` and callers convert. (3) **v1.8.12 direct backfill** — `scanDirectory` marks a file scanned even when it parses to nothing (deliberate: else every local/CPU/teams replay re-parses forever), so widening supported modes can't reach old files. `PARSER_CAPABILITY_VERSION` (`parser.ts`, now `2`) vs persisted `srs_parserCapabilityVersion` triggers a one-time `pruneUnproductiveScannedFiles` (`db.ts`) that drops scan marks for files that produced no game row; the startup scan then re-parses them. Ingested **4,878 direct games** on Joey's machine. (4) **Overlay bugfix (pre-existing since v1.8.7):** `statsOverlayPreview` snapshots the payload when a simulation starts, and `show` was never re-applied from the live store the way `layout` was — so ALL 12 "Show on overlay" toggles were inert during a simulation, on the real OBS overlay as well as the in-app preview. Fixed in `App.svelte` + `LiveRankedSession.svelte`. (5) Live Session tab: opponent **rank medal** (reuses `RANK_MEDAL_SVGS`) + **tag**, proportional grid (was `justify-content: space-between` + a `1fr` stage column, which sprawled on wide windows), compact rows, unranked runs capped to the latest 10 games. Tier colour now uses `opponent_tier_color` (recomputing from rating alone dropped Grandmaster to a Master colour). (6) **`vite.config.ts`: node polyfills are now disabled under vitest** (`process.env.VITEST`) — they stub `node:fs`, which blocked tests from reading real `.slp` fixtures; the production bundle still gets them.
- **v1.8.14 changes:** (1) **Opponent season W/L + last-season rank on the Live Session tab.** Both come from the `fetchRatingSnapshot(opponentCode)` call the live card ALREADY makes — `rankedNetplayProfileHistory` was being returned and discarded, same story as the characters in v1.8.8, so this costs **zero extra API calls**. `SeasonData` gained `global_rank` (from `dailyGlobalPlacement`); new `previousSeason()` in `api.ts` picks the most recently *completed* season. ⚠ **The API omits seasons a player didn't play**, so this is "their last season", NOT necessarily the one before the current one (verified live: a lapsed profile's newest entry was Season 2 while Season 4 was the latest season overall) — which is why the UI prints the season NAME rather than saying "last season". `dailyGlobalPlacement` **is** populated for past seasons and still only for ~top-300 (every observed value ≤ 288 across a real-profile sample), so it doubles as the Grandmaster flag exactly as it does for the current season. New `PrevSeasonInfo` type + `opponent_prev_season` on `ActiveSet` (`store.ts`); rendered as a full-width footer strip under the NOW PLAYING grid (the identity column is already the tallest of the three) with a 20px rank medal. The three duplicated opponent-profile `.then()` blocks in `watcher.ts` are now one `applyOpponentProfile()`. **Not on the OBS overlay** — deliberate scope call, would need its own visibility toggle. (2) **The remaining opponent-detection delay was the fs watcher, not the parser.** `watch()` from `@tauri-apps/plugin-fs` passes **`delayMs: 2000` when the option is omitted**, and the Rust side feeds it straight into `notify-debouncer-full` with `tick_rate: None` (= timeout/4); that debouncer holds EVERY event for the full timeout, so the `create` event driving the v1.8.13 game-start peek was delivered **2.0–2.5s late**. Now `delayMs: WATCH_DEBOUNCE_MS` (100ms) → ~100–125ms. A short delay still coalesces Slippi's 60fps write storm (the debouncer collapses same-kind events per path), it just stops charging that latency to the create event. ⚠ **Never omit `delayMs` on `watch()`** — the default is not "no debounce". `HEADER_PEEK_DELAYS_MS` retuned to `[50,100,200,400,800,1500,3000]` (we now often beat Slippi to the Game Start block) and an off-by-one fixed — it indexed `[attempt+1]`, silently skipping the shortest delay. (3) **Unranked players no longer shown with a rank.** Slippi starts everyone at **1100**, which sits inside Silver I (floor 1054.87), so an unplaced player was displayed as a genuine Silver I. `getRankTier` gained an optional 3rd arg `setsPlayed` (season W+L); below `PLACEMENT_SETS_REQUIRED` (5) it returns Unranked. **Defaults to `null` = rule skipped**, so paths without the count are unchanged. Wired into `applyOpponentProfile`/`opponentPrevSeason` (watcher), the overlay payload (`store.ts`), `Header.svelte`, `Sidebar.svelte`; the live card also hides the rating number when Unranked. ⚠ **The 5 is OUR reimplementation** — GraphQL introspection is disabled on internal.slippi.gg and `NetplayProfile` has no `rank`/`isPlaced`/`placementsRequired` field (slippi.gg derives the tier client-side too), so it can't be confirmed against the API. ⚠ **"rating == 1100" is NOT a valid placed/unplaced test** — 18/18 sampled zero-set profiles sat at exactly 1100, but a 3-set player was at 1095.6, so Slippi *does* publish a moving rating during placements. Use the set count. (4) **Net Rating per session on the Ranked Sessions tab.** New `sessionRatingDelta()` in `store.ts` → a stat card in `SessionView.svelte` (grid widens 8→9 when present) + a delta in the `RankedSessions.svelte` list rail. Brackets the session between snapshots, then **validates the bracket against the snapshots' season W/L** (sets between the two must equal the session's set count) so a stale baseline can't absorb another session's results. Sparse by design: only **19 of 137** real sessions have snapshot coverage and **16** pass validation — the card is dropped otherwise rather than showing an approximate number. (5) Folds in the three banked post-v1.8.13 changes below (stage table, non-legal-stage exclusion, scrollable games list).
- **v1.9.0 changes:** (1) **Opponent + matchup scouting notes** — see the bullet below, which shipped in this release. (2) **`lead_maintenance_rate` was capped at 0.58 for every player, always.** Two compounding defects: the size nudge docked you by the PEAK lead (`marginMax`) rather than by what you gave back, and `troughAfterUp` is structurally pinned at +1 (stocks are lost one at a time, so the moment you first go ahead the margin is exactly +1 and the running minimum records it) — which made `leadCbPos`'s `m >= 2 → 1.0` branch dead code. Since not dipping requires never losing a stock, which forces `marginMax` to 4, a flawless game took the maximum penalty: `0.70 − 0.04×3 = 0.58`. Now measured as the worst **drawdown from the running peak** (`maxGivenBack` / `lowAtGiveBack`); surrendering none = 1.0. ⚠ **A margin only counts as a lead while `opp.stocks > 0`** — every won game ends with you ahead, so without that gate a game you trailed the whole way and then closed out scored a free 100 ("went ahead, never gave any back"), which made a real 1–2 SET LOSS average to 100. Same gate mirrored on `playerAlive` for comeback. `leadMaintenanceDegree`/`comebackDegree` are now exported from `slp_parser.ts` and unit-tested in `lead-comeback.test.ts` over **stock-count paths**, not margins — the `oppAlive` rule can't be expressed in margins alone, and both bugs here came from feeding the formula a mislabelled quantity. `GRADING_LOGIC_VERSION` 6→7 (regrades stored grades). **No rescan needed** — it's in `ABSOLUTE_STATS`, never benchmarked, and `parse_hf_replays.py` only stores a crude win/loss binary for it. (3) **Live Session layout:** session figures are a compact strip at the top instead of stat cards at the bottom; the tab is a two-column grid (`repeat(auto-fit, minmax(360px, 1fr))`) with the live match left and notes right. The **Rolling 20-Set Win Rate** chart moved to the Rating History tab (it's an all-time trend, not a session number). (4) **Ctrl +/− now uses Chromium's `zoom`, not `transform: scale()`** — `transform` scales rendered output without reflowing, hence soft text, the `100/zoom vw/vh` counter-hack, and charts never resizing (ResizeObserver saw the untransformed size). Persisted as `srs_uiZoom`. (5) **Chart resize coalesced to one `chart.resize()` per animation frame** in `LineChart`/`BarChart` — it was running a full ECharts re-layout on every `ResizeObserver` callback during a window drag. (6) **20-snap average is toggleable** on Rating History (`srs_showRatingAvg`, defaults on) via a chip in the card header. ⚠ It was first built as a clickable legend entry, which rendered identically to the static label it replaced and was completely undiscoverable — controls go in the control row, legends describe.
- **v1.9.0 also fixes `comeback_rate`, which had the exact mirror of the lead bug.** `highAfterDown` was a running maximum seeded at the −1 entry point, so the base could never fall below `leadCbPos(-1) = 0.30` — the floor was unreachable exactly as the lead's ceiling was — and the depth nudge then *added* credit for how deep the hole was even when none of it had been climbed out of. Net effect: being 4-stocked scored **31.5** while going down one and never recovering scored **22.5**, so a blowout beat a close loss. Now the mirror of the lead measure: the best **run-up from the running trough** (`maxClawedBack` / `highAtClawBack`), gated on `playerAlive` so being taken to zero isn't "falling further behind". Recovering nothing = 0. Folded into the same `GRADING_LOGIC_VERSION` 7 bump, so it cost no extra regrade. ⚠ Getting four-stocked is the ONLY path that recovers nothing — taking any of their stocks is a recovery — so don't expect to write a "shallow no-recovery" test case; it can't exist.
- **Shipped in v1.9.0 (was banked): opponent + matchup scouting notes.** Quick bullets you write for yourself, surfaced on the Live Session tab the moment an opponent is detected (which since v1.8.13/v1.8.14 is at game *start*, so they're up before the match loads). Two kinds in one table: **opponent** (keyed by connect code) and **matchup** (keyed by *your* char → *their* char, **directional**; `player_char = ''` is a wildcard meaning "any character I play"). Storage is a **new `notes.db`, deliberately NOT the per-connect-code DB** (`getNotesDb()` in `db.ts`, same pattern as `scanned.db`) — a note is about a person, not about which of your accounts faced them, so linked codes share notes and switching your primary code can't strand them. Absent keys are `''` not `NULL` so lookups are plain equality on the indexes. **One row = one bullet**, and the whole table is held in memory (`notes` store in `src/lib/notes.ts`, loaded in `App.svelte`'s `onMount`, not lazily on tab open) so the live panel renders synchronously the instant the opponent appears. Code: `src/lib/notes.ts` (pure logic + stores + actions; imports `store.ts`, never the reverse — the writable can't live in `store.ts` or the derived's use of `notesForContext` is a cycle), `NoteList.svelte` / `NoteComposer.svelte` / `OpponentNotes.svelte`, new **Notes tab** `src/components/tabs/Notes.svelte`. ⚠ The tab was **appended** as index 7 rather than placed next to Live Session because `activeTab` is a persisted integer index. ⚠ **Notes are deliberately NOT on the OBS overlay** — it's a scouting report on the person you're playing. **The Notes tab ships FREE to everyone — decided 2026-08-04, not an oversight.** `Notes.svelte` has no `PremiumGate` and should not gain one. The live in-session panel is Premium only as a side effect of living inside the Live Session tab's `$isPremium` gate: free users write and read notes on the tab, premium users additionally get them surfaced automatically the moment an opponent appears.
- **Banked on `main`, NOT yet released (post-v1.8.13 — ⚠ SHIPPED IN v1.8.14, see above):** (1) the `STAGES` table in `parser.ts` was **wrong for every non-tournament-legal stage** — only 2/3/8/28/31/32 were correct, ~12 were mapped to the wrong name and ~13 were missing. Unreachable until direct-connect replays started being ingested (v1.8.12/v1.8.13), since ranked/unranked only produce the six legal stages. Now mirrors slippi-js's `Stage` enum, pinned by `parser.test.ts`. (2) **Non-legal stages are excluded from all statistics and grading** via `LEGAL_STAGES`/`isLegalStage()` (`parser.ts`), filtered in `filteredGames` (every stat derives from it) plus both grading paths — the benchmarks are entirely legal-stage ranked play. Games are still parsed and stored; aggregates only, so it's reversible. The NOW PLAYING running scoreboard deliberately still counts them (it's a scoreboard, not a statistic). (3) **The Live Session per-game list shows every game of the match instead of the latest 10** — `.game-list` scrolls with a `clamp(140px, 48vh, 620px)` cap (tall window stretches, short one scrolls), sticky column headers live *inside* the scroll container so they stay aligned when the scrollbar appears, and the list auto-follows the newest game unless the user has scrolled up. Fold all three into the next release with a version bump.
- Before building anything that touches grade history persistence or premium gating, discuss the approach first.

## Working across machines

This repo is developed on more than one machine, and **git is the only thing that syncs.** Claude's auto-memory (`~/.claude/`), the app's local SQLite DBs, and `.venv/` do NOT travel between machines — never treat memory as shared truth (that's how the "grading is dev-only" note went stale).

- **Start of session:** `git pull`, read `docs/dev_notes.md` (handoff banner + NEXT UP), then `git status`.
- **End of session:** commit + push everything. If a feature's status or gating changed, update this file in the **same commit**.
- Full checklist + what does/doesn't travel: `docs/dev_notes.md` → "Cross-machine workflow".
